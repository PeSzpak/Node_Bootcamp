<!DOCTYPE html>
<html lang="pt-br">
  <%- include('./partials/head.ejs')%>
  <body>
    <%- include('./partials/navbar.ejs') %>
    <div class="content">
      <div class="container">
        <% if (users && users.length > 0 ) { %>
          <% users.forEach(user => { %>
            <div class="user-card">
              <h3 class="title"><%= user.firstName %> <%= user.lastName %></h3>
              <p class="meta"><%= user.email %></p>
            </div>
          <% }) %>
        <% } else { %>
          <div class="empty">Não há usuários para exibir...😴</div>
        <% } %>
      </div>
    </div>
  </body>
</html>
