<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>usuarios</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
        "Helvetica Neue", Arial;
      margin: 0;
      background: #f5f7fb;
      color: #222;
    }
    .content {
      padding: 40px 0;
      display: flex;
      justify-content: center;
    }
    .container {
      width: 100%;
      max-width: 920px;
      background: #fff;
      border-radius: 10px;
      box-shadow: 0 6px 18px rgba(42, 52, 70, 0.08);
      padding: 24px;
    }

    .title {
      margin: 0 0 6px 0;
      font-size: 20px;
      font-weight: 600;
      color: #0f1724;
    }
    p {
      margin: 0 0 14px 0;
      color: #4b5563;
    }

    .user-card {
      padding: 12px 14px;
      border-radius: 8px;
      border: 1px solid #eef2f7;
      margin-bottom: 12px;
      background: linear-gradient(180deg, #ffffff 0%, #fbfdff 100%);
    }
    .user-card .meta {
      font-size: 13px;
      color: #6b7280;
    }

    .empty {
      text-align: center;
      padding: 28px 12px;
      color: #9ca3af;
    }

    .pagTitle {
      font-size: 28px;
      margin: 0 0 18px 0;
      color: #0b1220;
      letter-spacing: 0.2px;
      font-weight: 800;
      text-align: center; 
      width: 100%;
      background-color: rgb(255, 255, 255);
    }
    @media (max-width: 600px) {
      .pagTitle {
        font-size: 20px;
      }
    }

    @media (max-width: 600px) {
      .container {
        padding: 16px;
        border-radius: 8px;
      }
      .title {
        font-size: 18px;
      }
    }
  </style>
</head>
